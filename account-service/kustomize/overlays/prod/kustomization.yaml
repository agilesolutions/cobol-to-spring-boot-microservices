apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: services
resources:
- ../../base
patches:
- patch: |-
    - op: replace
      path: /spec/replicas
      value: 3
  target:
    kind: Deployment
    name: account-service
images:
- name: account-service
  newName: agilesolutions/account-service:1.0.0
labels:
- includeSelectors: true
  pairs:
    env: prod
